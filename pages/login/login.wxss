.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  gap: 32rpx;
}
.title { font-size: 56rpx; font-weight: bold; margin-bottom: 16rpx; }
.subtitle { font-size: 36rpx; color: #666; }
.role-btn { width: 60%; font-size: 36rpx; }
