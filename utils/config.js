const API_BASE_URL = 'https://timetable.hotmiya.cn';

// Set to true when testing in WeChat DevTools without a real developer login
// (tourist/guest mode). Switch to false before submitting to WeChat review.
const DEV_MOCK_LOGIN = false;

module.exports = { API_BASE_URL, DEV_MOCK_LOGIN };
